import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.GestionEtudiant',
  appName: 'GestionEtudiant',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
